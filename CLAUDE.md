# Claude 项目入口

Claude 修改本项目时必须先阅读并遵守 `AGENTS.md`。本文件只作为 Claude 的入口提醒，不维护第二套规则。

## 执行要求

1. 先读 `AGENTS.md`。
2. 按任务类型读取 `docs/README.md` 中列出的规范文档。
3. 修改前先定位相关代码、模块/包 README、API 文档和测试。
4. 修改后同步文档并运行必要测试。
5. 完成前按 `docs/guides/self-checklist.md` 自检。

