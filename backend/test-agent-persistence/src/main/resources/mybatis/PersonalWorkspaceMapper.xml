<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icbc.testagent.persistence.mybatis.PersonalWorkspaceMapper">
    <update id="updateLocation">
        update personal_workspaces
        set branch = #{branch},
            repo_root_path = #{repoRootPath},
            workspace_root_path = #{workspaceRootPath},
            base_commit = #{baseCommit},
            updated_at = #{updatedAt}
        where personal_workspace_id = #{personalWorkspaceId}
    </update>
</mapper>
