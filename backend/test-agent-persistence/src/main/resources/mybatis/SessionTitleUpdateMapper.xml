<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.testagent.persistence.mybatis.SessionTitleUpdateMapper">

    <update id="updateTitleIfCurrent">
        update sessions
        set title = #{title},
            updated_at = #{updatedAt},
            trace_id = #{traceId}
        where session_id = #{sessionId}
          and title = #{expectedTitle}
    </update>

</mapper>
