<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icbc.testagent.persistence.mybatis.CommonParameterMapper">
    <resultMap id="CommonParameterRowMap" type="com.icbc.testagent.persistence.mybatis.CommonParameterRow">
        <constructor>
            <arg column="parameter_id" javaType="java.lang.String"/>
            <arg column="parameter_english" javaType="java.lang.String"/>
            <arg column="parameter_chinese" javaType="java.lang.String"/>
            <arg column="parameter_value" javaType="java.lang.String"/>
            <arg column="platform" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.Instant"/>
            <arg column="updated_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <sql id="CommonParameterColumns">
        parameter_id, parameter_english, parameter_chinese, parameter_value, platform, created_at, updated_at
    </sql>

    <select id="findByEnglishNameAndPlatform" resultMap="CommonParameterRowMap">
        select
        <include refid="CommonParameterColumns"/>
        from common_parameters
        where parameter_english = #{englishName}
          and platform = #{platform}
    </select>

    <select id="findAll" resultMap="CommonParameterRowMap">
        select
        <include refid="CommonParameterColumns"/>
        from common_parameters
        order by parameter_english, platform
    </select>

    <select id="findByParameterId" resultMap="CommonParameterRowMap">
        select
        <include refid="CommonParameterColumns"/>
        from common_parameters
        where parameter_id = #{parameterId}
    </select>

    <update id="updateValue">
        update common_parameters
        set parameter_value = #{value},
            updated_at = #{updatedAt}
        where parameter_id = #{parameterId}
    </update>
</mapper>
