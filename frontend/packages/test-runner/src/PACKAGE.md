# 包说明：@test-agent/test-runner/src

## 职责

展示运行状态、日志和 Run 操作入口。

## 主要程序清单

- `TestRunnerPanel.tsx`：底部运行面板。

## 允许依赖

- React。
- `@test-agent/shared-types`。
- `@test-agent/ui-kit`。

## 禁止依赖

- backend-api、event-stream-client、shell 命令执行。

## 修改时必须同步更新

- 本包 README。
- `docs/frontend/frontend-testing-standards.md`。
