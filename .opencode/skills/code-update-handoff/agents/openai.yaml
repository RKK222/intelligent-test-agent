interface:
  display_name: "Code Update Handoff"
  short_description: "Candid handoff and session log after edits"
  default_prompt: "Use $code-update-handoff to close a code-change batch with a candid handoff and an updated session log."
